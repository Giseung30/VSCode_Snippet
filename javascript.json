{
  "sfor": {
    "prefix": "sfor",
    "body": [
      "for (let ${1:i} = 0, ${2:l} = ${3:0}; $1 < $2; ++$1) {",
      "    $4",
      "}"
    ],
    "description": "for문을 만듭니다"
  },
  "slog": {
    "prefix": "slog",
    "body": [
      "console.log(`$1`);$2"
    ],
    "description": "console.log문을 만듭니다"
  }
}